[project]
name = "summerjob-planner"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.0.0",
    "pika>=1.3.2",
    "psycopg2-binary>=2.9.10",
    "PuLP>=3.2.1",
    "pandas>=2.2.3",
    "SQLAlchemy>=2.0",
]

[project.optional-dependencies]
dev = ["ruff>=0.0.0"]

[dependency-groups]
dev = ["ruff>=0.0.0"]
