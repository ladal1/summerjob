'use client'

export default function MyParticipateFromPosts() {
  /*const plans = deserializeMyPlans(sPlan)
  const { data, error, isLoading } = useAPIMyPlans({
    fallbackData: plans,
  })*/

  return <></>
}
